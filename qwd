local Player = game.Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local Root = Character:WaitForChild("HumanoidRootPart")

-- Simpan posisi asli
local Pos = Root.CFrame

-- Eksekusi Invisible Glitch
Character:MoveTo(Vector3.new(0, -500, 0)) -- Kirim ke bawah map sebentar
task.wait(0.5)

-- Hapus sambungan karakter (Ini yang bikin kamu Invis di server)
if Character:FindFirstChild("LowerTorso") then
    Character.LowerTorso.Root:Destroy()
else
    Character.Torso.Neck:Destroy()
end

-- Kembalikan posisi ke tempat semula
Root.CFrame = Pos

print("Invis Glitch Aktif! Di layar orang lain kamu sudah hilang atau diam.")
