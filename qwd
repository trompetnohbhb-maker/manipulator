local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")

-- [[ 1. WADAH UTAMA ]] --
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "YutaHub_PastiMuncul"
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 160, 0, 250) -- Ukuran ditambah dikit biar muat
MainFrame.Position = UDim2.new(0.5, -80, 0.4, 0) -- Muncul di tengah
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true -- Pakai fitur bawaan roblox aja biar gk ribet

local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 8)

-- Judul
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "YUTA HUB"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 14
Instance.new("UICorner", Title)

-- Wadah Tombol (Pake Frame biar rapi)
local ButtonHolder = Instance.new("Frame", MainFrame)
ButtonHolder.Size = UDim2.new(1, 0, 1, -40)
ButtonHolder.Position = UDim2.new(0, 0, 0, 40)
ButtonHolder.BackgroundTransparency = 1

local Layout = Instance.new("UIListLayout", ButtonHolder)
Layout.Padding = UDim.new(0, 5)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- [[ 2. FUNGSI TOMBOL ]] --
local function CreateButton(name, order, callback)
    local btn = Instance.new("TextButton", ButtonHolder)
    btn.Size = UDim2.new(0.9, 0, 0, 35)
    btn.LayoutOrder = order
    btn.Text = name .. ": OFF"
    btn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 13
    Instance.new("UICorner", btn)

    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        btn.Text = name .. (active and ": ON" or ": OFF")
        btn.BackgroundColor3 = active and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(150, 0, 0)
        task.spawn(function()
            callback(active)
        end)
    end)
end

-- [[ 3. ISI FITUR (OTAKNYA) ]] --

-- AUTO ATTACK (Click Tanpa Delay)
CreateButton("Auto Attack", 1, function(state)
    _G.AA = state
    while _G.AA do
        game:GetService("RunService").RenderStepped:Wait()
        pcall(function()
            if LocalPlayer.Character:FindFirstChildOfClass("Tool") then
                VirtualUser:CaptureController()
                VirtualUser:ClickButton1(Vector2.new(0,0))
            end
        end)
    end
end)

-- ATTACK RANGE (Mob Pull)
CreateButton("Attack Range", 2, function(state)
    _G.Range = state
    while _G.Range do
        task.wait()
        pcall(function()
            if _G.AA then -- Hanya narik pas lagi nyerang
                for _, v in pairs(workspace.Enemies:GetChildren()) do
                    if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                        v.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, -10, 0)
                        v.HumanoidRootPart.CanCollide = false
                    end
                end
            end
        end)
    end
end)

-- FLY ABOVE NPC (Tinggi 25)
CreateButton("Fly Above NPC", 3, function(state)
    _G.Fly = state
    while _G.Fly do
        task.wait()
        pcall(function()
            for _, v in pairs(workspace.Enemies:GetChildren()) do
                if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                    LocalPlayer.Character.HumanoidRootPart.CFrame = v.HumanoidRootPart.CFrame * CFrame.new(0, 25, 0)
                    break
                end
            end
        end)
    end
end)

-- AUTO QUEST
CreateButton("Auto Quest", 4, function(state)
    _G.Quest = state
    while _G.Quest do
        task.wait(1.5)
        pcall(function()
            local comm = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
            if comm and not LocalPlayer.PlayerGui.Main.Quest.Visible then
                comm:InvokeServer("StartQuest", "BanditQuest1", 1)
            end
        end)
    end
end)

print("Yuta Hub: Muncul & Bisa di-drag!")
