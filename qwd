local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- KONFIGURASI AIM
local AimSettings = {
    Enabled = true,
    AimPart = "HumanoidRootPart", -- Bisa ganti ke "HumanoidRootPart"
    Smoothness = 0.1, -- Semakin kecil semakin cepat (0.1 - 0.5)
    FOV = 900,
    AimKey = Enum.UserInputType.MouseButton1 -- Aktif saat KLIK KIRI (Nembak)
}

-- [[ 1. ESP STABIL (LOGIKA ASLI KAMU) ]] --
local function buatESP(player)
    player.CharacterAdded:Connect(function(char)
        if player == LocalPlayer then return end
        local root = char:WaitForChild("HumanoidRootPart", 10) 
        if root then
            if root:FindFirstChild("KotakESP") then root.KotakESP:Destroy() end
            local gui = Instance.new("BillboardGui", root)
            gui.Name = "KotakESP"
            gui.AlwaysOnTop = true
            gui.Size = UDim2.new(4, 0, 5.5, 0)
            local frame = Instance.new("Frame", gui)
            frame.Size = UDim2.new(1, 0, 1, 0)
            frame.BackgroundTransparency = 0.6
            frame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
            local stroke = Instance.new("UIStroke", frame)
            stroke.Thickness = 2
            stroke.Color = Color3.fromRGB(255, 255, 255)
        end
    end)
end

-- [[ 2. LOGIKA SMART AIMBOT ]] --
local function getClosestPlayer()
    local target = nil
    local shortestDistance = AimSettings.FOV

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild(AimSettings.AimPart) then
            local part = player.Character[AimSettings.AimPart]
            local pos, onScreen = Camera:WorldToViewportPoint(part.Position)
            
            if onScreen then
                local mousePos = UserInputService:GetMouseLocation()
                local distance = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                
                if distance < shortestDistance then
                    target = part
                    shortestDistance = distance
                end
            end
        end
    end
    return target
end

RunService.RenderStepped:Connect(function()
    if AimSettings.Enabled and UserInputService:IsMouseButtonPressed(AimSettings.AimKey) then
        local target = getClosestPlayer()
        if target then
            -- Bypass "Satu Ketuk": Mengunci kamera dengan Lerp agar peluru sinkron
            local lookAt = CFrame.new(Camera.CFrame.Position, target.Position)
            Camera.CFrame = Camera.CFrame:Lerp(lookAt, AimSettings.Smoothness)
        end
    end
end)

-- Jalankan ESP untuk pemain lama dan baru
for _, p in pairs(Players:GetPlayers()) do
    if p.Character then buatESP(p) end
    buatESP(p)
end
Players.PlayerAdded:Connect(buatESP)
