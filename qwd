local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

-- [[ 1. RESET & SETUP UI ]] --
_G.AutoAttack = false
_G.AttackRange = false
_G.FlyAbove = false

local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
if PlayerGui:FindFirstChild("YutaHub_V4") then PlayerGui.YutaHub_V4:Destroy() end

local ScreenGui = Instance.new("ScreenGui", PlayerGui)
ScreenGui.Name = "YutaHub_V4"
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 160, 0, 260)
MainFrame.Position = UDim2.new(0.5, -80, 0.4, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local Header = Instance.new("TextLabel", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 40)
Header.Text = "YUTA HUB 4" -- UPDATE NAMA
Header.TextColor3 = Color3.fromRGB(255, 255, 255)
Header.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
Header.Font = Enum.Font.SourceSansBold
Header.TextSize = 16
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 10)

-- Drag System
local dragging, dragStart, startPos
Header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = MainFrame.Position
    end
end)
game:GetService("UserInputService").InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
game:GetService("UserInputService").InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
end)

local ButtonHolder = Instance.new("Frame", MainFrame)
ButtonHolder.Size = UDim2.new(1, 0, 1, -45); ButtonHolder.Position = UDim2.new(0, 0, 0, 45); ButtonHolder.BackgroundTransparency = 1
local Layout = Instance.new("UIListLayout", ButtonHolder); Layout.Padding = UDim.new(0, 7); Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function CreateButton(name, order, callback)
    local btn = Instance.new("TextButton", ButtonHolder)
    btn.Size = UDim2.new(0.9, 0, 0, 40); btn.LayoutOrder = order; btn.Text = name .. ": OFF"
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35); btn.TextColor3 = Color3.fromRGB(255, 255, 255); btn.Font = Enum.Font.SourceSansBold
    Instance.new("UICorner", btn)
    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        btn.Text = name .. (active and ": ON" or ": OFF")
        btn.BackgroundColor3 = active and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(35, 35, 35)
        callback(active)
    end)
end

-- [[ 2. SUPER FAST ATTACK ENGINE ]] --
local function GetEnemy()
    local folder = workspace:FindFirstChild("Enemies") or workspace:FindFirstChild("Characters")
    for _, v in pairs(folder:GetChildren()) do
        if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and (v.HumanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude < 100 then
            return v
        end
    end
end

-- Fungsi Serang Tanpa Cooldown
local function AttackNoCooldown()
    pcall(function()
        local AttackRE = ReplicatedStorage.Modules.Net:FindFirstChild("RE/Attack") or ReplicatedStorage.Modules.Net:FindFirstChild("Attack")
        local Tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if Tool and AttackRE then
            -- Spam FireServer berkali-kali untuk Super Attack
            AttackRE:FireServer(0.1)
            AttackRE:FireServer(0.1)
            AttackRE:FireServer(0.1) -- Stack 3 hit sekaligus
            
            -- Memicu animasi internal agar tidak dicurigai
            if Tool:FindFirstChild("Attack") then
                Tool.Attack:Fire()
            end
        end
    end)
end

-- [[ 3. FITUR ]] --

-- SUPER AUTO ATTACK (Fast Method)
CreateButton("Auto Attack", 1, function(state)
    _G.AutoAttack = state
    task.spawn(function()
        while _G.AutoAttack do
            -- Menggunakan Heartbeat agar hit teregistrasi sangat cepat
            RunService.Heartbeat:Wait()
            if _G.AutoAttack then
                AttackNoCooldown()
            end
        end
    end)
end)

-- ATTACK RANGE (Hitbox Expander)
CreateButton("Attack Range", 2, function(state)
    _G.AttackRange = state
    task.spawn(function()
        while _G.AttackRange do
            task.wait(1)
            pcall(function()
                local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
                if tool and tool:FindFirstChild("Handle") then
                    tool.Handle.Size = Vector3.new(80, 80, 80)
                    tool.Handle.Transparency = 0.8
                    tool.Handle.CanCollide = false
                end
            end)
        end
    end)
end)

-- FLY ABOVE NPC (Tinggi 8 Unit biar hit pas)
CreateButton("Fly Above NPC", 3, function(state)
    _G.FlyAbove = state
    task.spawn(function()
        while _G.FlyAbove do
            RunService.Heartbeat:Wait()
            pcall(function()
                local Enemy = GetEnemy()
                if Enemy and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    LocalPlayer.Character.HumanoidRootPart.CFrame = Enemy.HumanoidRootPart.CFrame * CFrame.new(0, 15, 0)
                end
            end)
        end
    end)
end)

-- AUTO QUEST (Bandit)
CreateButton("Auto Quest", 4, function(state)
    _G.AutoQuest = state
    task.spawn(function()
        local comm = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
        while _G.AutoQuest do
            if comm and not LocalPlayer.PlayerGui.Main.Quest.Visible then
                comm:InvokeServer("StartQuest", "BanditQuest1", 1)
            end
            task.wait(1.5)
        end
    end)
end)
